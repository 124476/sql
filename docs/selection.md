\# Выборка данных



FROM — откуда брать данные

WHERE — как фильтровать строки

SELECT — какие столбцы выбрать

DISTINCT — оставить только уникальные строки

ORDER BY — упорядочить результат

LIMIT / OFFSET — ограничить и сместить количество строк

